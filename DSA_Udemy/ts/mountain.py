def band(nums):
    dp={}
    for x in nums:
        dp.set(x,False)
    ans=0
    for x in nums:
        if (dp.get(x)==True):
            continue
        else:
            curr_ans=1
            left=x-1
            while (dp.find())
    
